{
  "name": "reclaim",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --import tsx --test tests/**/*.test.ts && npm run check:web",
    "typecheck": "tsc --noEmit",
    "check:web": "node --check web/config.js && node --check web/api.js && node --check web/app.js"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
